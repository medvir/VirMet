# bandit.yaml
skips:
  # B404: Consider possible security implications.
  # This is a bioinformatics tool, subprocess is expected.
  - B404 
  
  # B101: Use of assert detected. This is OK for internal checks.
  - B101

  # B603: subprocess call - check for execution of untrusted input.
  # Most imputs are generated by VirMet itself and should be OK.
  - B603